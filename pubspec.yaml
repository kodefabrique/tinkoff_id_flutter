name: tinkoff_id_flutter
description: Tinkoff ID Native Android And iOS Flutter Realization
version: 1.0.0
homepage: kodefabrique.com

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.kodefabrique.tinkoff_id_flutter
        pluginClass: TinkoffIdFlutterPlugin
      ios:
        pluginClass: TinkoffIdFlutterPlugin

